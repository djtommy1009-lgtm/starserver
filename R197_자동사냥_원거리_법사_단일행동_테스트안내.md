# R197 자동사냥 원거리·법사 단일 행동 슬롯 테스트

## 목표

자동사냥 캐릭터가 수동 유저와 같은 전투 순서를 사용하도록 다음 회귀를 테스트 서버에서 먼저 차단합니다.

- 요정 기본 활질과 트리플 애로우가 같은 공격 슬롯에서 겹치는 현상
- 몬스터 사망 뒤 트리플 또는 활질이 남는 현상
- 화면상 바라보는 몬스터와 실제 피해 대상이 달라 보이는 현상
- 클라이언트가 아직 인식하지 못한 몬스터를 향한 허공 활질
- 법사 기본 지팡이 공격과 공격마법이 같은 슬롯에서 연속 실행되는 현상
- 대상 사망·전환 뒤 이전 공격마법이 밀려 나오는 현상

## 변경 범위

실제 Java 수정 대상은 다음 한 파일입니다.

```text
src/l1j/server/AutoHuntSystem/AutoHuntController.java
```

변경하지 않는 항목:

```text
DB
자동사냥 맵 및 스폰
가상방 정책
공격거리
공격속도
대미지
드랍
경험치
StarGuard
운영 l1jserver.jar
```

## 수정 규칙

### 요정

한 공격 슬롯에서는 아래 둘 중 하나만 실행합니다.

```text
트리플 애로우 1회
또는
기본 활 공격 1회
```

트리플을 사용한 슬롯에서는 기본 활 공격을 추가로 보내지 않습니다.

### 법사

한 공격 슬롯에서는 아래 둘 중 하나만 실행합니다.

```text
공격마법 1회
또는
기본 지팡이 공격 1회
```

공격마법을 시도한 슬롯에서는 기본공격을 추가로 보내지 않습니다. 대상이 바뀌었다는 이유만으로 4초 마법 주기를 0으로 초기화하지 않습니다.

### 공통 최종 검사

행동을 보내기 직전에 다음을 다시 확인합니다.

```text
현재 자동사냥 컨트롤러가 유효함
대상이 현재 자동 타깃과 동일함
대상이 살아 있음
HP가 1 이상임
같은 맵임
L1World 정본 객체가 같은 인스턴스임
캐릭터가 대상을 KnownObject로 인식함
공격 가능 거리와 시야가 유효함
```

사망 타깃을 발견한 틱에는 다음 타깃을 찾을 수 있지만 공격은 다음 정상 슬롯에서 실행합니다.

## 실행 순서

### 1. 기준 테스트 서버 가동 및 재현

수정 전 테스트 서버에서 요정·법사 현상을 영상으로 1회 남깁니다. 채팅에 아래 표식을 순서대로 입력하면 영상과 로그 시점을 맞추기 쉽습니다.

```text
R197수정전시작
R197요정테스트
R197법사테스트
R197수정전종료
```

### 2. 후보 JAR 빌드

테스트 서버가 실행 중이어도 후보 빌드는 가능합니다. 다음 파일을 실행합니다.

```text
tools\1_R197_후보JAR_빌드.cmd
```

빌더는 다음 검사를 통과해야만 후보 JAR을 만듭니다.

1. 운영 폴더가 아닌지 확인
2. 현재 소스 전체 컴파일
3. 현재 소스의 `AutoHuntController` 바이트코드와 테스트 JAR 바이트코드 대조
4. 일치할 때만 Java 1개 수정
5. 전체 재컴파일
6. R197 메서드·필드 검수
7. 테스트 JAR을 복사한 후보 JAR에 `AutoHuntController*.class`만 반영
8. 후보 JAR과 컴파일 CLASS 바이트코드 재대조

소스와 테스트 JAR이 다르면 소스를 자동 원복하고 중단합니다.

### 3. 테스트 서버 정상 종료

기존 테스트 서버 종료 기능을 사용합니다. 테스트 서버용 `java.exe`가 완전히 종료됐는지 확인합니다.

### 4. 후보 JAR 테스트 서버 적용

```text
tools\2_R197_테스트서버_적용.cmd
```

적용기는 다음을 수행합니다.

- 테스트 JAR 자동 백업
- 후보 JAR SHA-256 확인
- R197 CLASS 표식 확인
- 테스트 JAR만 원자 교체
- 적용 후 SHA-256 재검수
- 운영 JAR 전후 해시 대조

운영 JAR은 변경하지 않습니다.

### 5. 패치 테스트 서버 가동

기존 테스트 서버 시작 파일로 실행합니다. 채팅 표식:

```text
R197수정후시작
R197요정테스트
R197법사테스트
R197수정후종료
```

### 6. 테스트 로그 수집

```text
tools\4_R197_테스트로그_수집.cmd
```

바탕화면에 다음 ZIP이 생성됩니다.

```text
R197_자동사냥전투_테스트로그_날짜_시각.zip
```

### 7. 문제 발생 시 테스트 JAR 원복

테스트 서버를 정상 종료한 뒤 실행합니다.

```text
tools\3_R197_테스트서버_원복.cmd
```

## 테스트 구성

### 요정

```text
활 장착
트리플 애로우 습득
화살 충분
몬스터가 좌우에 동시에 존재하는 장소
몬스터 1~3타 이내 사망 조건 포함
```

확인 항목:

```text
트리플 슬롯에 기본 활질이 겹치지 않음
죽은 몬스터에 트리플·활질이 이어지지 않음
새 타깃 공격이 이전 타깃 모션과 겹치지 않음
화면에 생성되지 않은 몬스터에 허공 활질하지 않음
```

### 법사

```text
자동사냥 공격마법 설정
기본 지팡이 공격 가능 상태
몬스터가 좌우에 동시에 존재하는 장소
몬스터 1~3타 이내 사망 조건 포함
```

확인 항목:

```text
공격마법 슬롯에 기본 지팡이 공격이 겹치지 않음
사망 타깃에 마법이 계속 나가지 않음
대상 변경 때마다 4초 주기가 즉시 초기화되지 않음
이전 타깃의 마법이 다음 타깃으로 밀려 보이지 않음
```

### 근거리 회귀 확인

기사 또는 근거리 캐릭터도 함께 둡니다.

```text
기존 한 칸 거리 제한 유지
붙기 전 칼질 재발 없음
기존 공속 유지
기존 길찾기·비비기 복구 기능 유지
```

## 시간별 검수

```text
즉시: 30분
단기: 3시간
중기: 12시간
장기: 24시간
최종: 10일
```

최종 합격 기준:

```text
기본 활 + 트리플 같은 슬롯 중복 0건
기본공격 + 법사 공격마법 같은 슬롯 중복 0건
사망 타깃 공격 0건
미인식 대상 공격 0건
공격 대상과 피해 대상 불일치 0건
스핵처럼 보이는 중첩 모션 0건
새 예외·서버 종료·접속 불가 0건
운영 JAR 변경 0건
```

## 브랜치

```text
r197-auto-ranged-single-action
```

후보가 장시간 검수를 통과하기 전에는 운영 서버에 병합하거나 적용하지 않습니다.
